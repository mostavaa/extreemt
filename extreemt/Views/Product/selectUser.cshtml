@{
    if (!extreemt.tools.isAuthorized(Request.Url.AbsolutePath))
    {
        if (Request.Url.AbsolutePath != Url.Action("Index", "Home"))
        {
            Response.Redirect(Url.Action("Index", "Home"));
            return;
        }
    }
}
@{
    ViewBag.Title = "selectUser";
    string encId = (string)ViewData["proId"];
}

<h2>select User</h2>
<form method="post" action="@Url.Action("Buy" , "Product" )?proId=@encId">
    <select class="form-control" name="wkala">
        <option value="1">001</option>
        <option value="2">002</option>
        <option value="3">003</option>
    </select>
    <input type="submit" class="btn btn-default" value="submit"/>
</form>
