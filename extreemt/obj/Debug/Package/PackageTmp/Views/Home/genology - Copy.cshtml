@using org.jivesoftware.util;
@{
    if (!extreemt.tools.isAuthorized(Request.Url.AbsolutePath))
    {
        if (Request.Url.AbsolutePath != Url.Action("Index", "Home"))
        {
            Response.Redirect(Url.Action("Index", "Home"));
            return;
        }
    }
}
@{
    ViewBag.Title = "genology";
}



@helper renderNode(extreemt.user user, string cssClass)
{<div class="@cssClass">
    @{
        string backGround = "white";
        if (user.status == "inActive")
        {
            backGround = "red";
        }
    }
        <div style="background-color:@backGround;text-align:center;border-style:solid;border-radius:10px">

            <div class="">
                <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
            </div>

            @{
                
                extreemt.crypt algo = new extreemt.crypt();
                string userId = algo.Encrypt(user.userId + "$$$" + user.genNumber);

                userId = HttpUtility.UrlEncode(userId.Replace("+", "25252"));
                string url = Url.Action("genology", "Home") + "?a=" + userId;
            }
            <div class=""><a href="@url">@user.userId</a> </div>

            <div class="">
                <span>@user.firstname @user.lastname</span>
            </div>

            <div class="greenrow">
                L: @user.leftActiveCount  R: @user.rightActiveCount
            </div>
            <div class="redrow">
                L: @user.leftInactiveCount R: @user.rightInactiveCount
            </div>

        </div>
    </div>
}







@section head{
    <style>
        .greenrow {
            font-family: Arial;
            background-color: MediumSeaGreen;
            text-align: center;
        }

        .redrow {
            width: 100%;
            font-family: Arial;
            background-color: #FF6666;
            text-align: center;
        }

        .col-md-1-nos {
            display: inline-block;
        }
    </style>
}
<h2>genology</h2>

@{
    string id, decryptedId;
        int currentUserId, GenNumber;
        extreemt.crypt algo;
    try
    {
         id = (string)ViewData["id"];

         algo = new extreemt.crypt();
         decryptedId = algo.Decrypt(id);
        string[] user = decryptedId.Split(new char[] { '$', '$', '$' });
        //string position = "parent";

         currentUserId = int.Parse(user[0]);
        int i = 1;
        while (user[i] == "")
        {
            i++;
        }
         GenNumber = int.Parse(user[i]);
    }
    catch (Exception c)
    {
        return;
    }


    if (currentUserId != null && currentUserId != 0)
    {

        extreemt.user leftUser = null;
        extreemt.user rightUser = null;
        extreemt.extreemtEntities db = new extreemt.extreemtEntities();

        extreemt.user currentUser = db.users.Where(u => u.userId == currentUserId && u.genNumber == GenNumber).First();
        extreemt.user loginUser = currentUser;
        if (currentUser.genNumber == 1)
        {
            if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 2).Count() > 0)
            {

                leftUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 2).First();
            }
            if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 3).Count() > 0)
            {

                rightUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 3).First();
            }
        }
        else if (currentUser.genNumber == 2)
        {
            if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 2).Count() > 0)
            {

                leftUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 2).First();
            }
            if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 2).Count() > 0)
            {

                rightUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 2).First();
            }
        }
        else
        {

            if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 3).Count() > 0)
            {

                leftUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 3).First();
            }
            if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 3).Count() > 0)
            {

                rightUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 3).First();
            }
        }

        <div class="container">
            <div class="row">
                <div class="col-md-offset-5 col-md-2">
                    <div style="background-color:white;text-align:center;border-style:solid;border-radius:10px">

                        <div class="">
                            <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                        </div>
                        @{
        string userId = algo.Encrypt(currentUser.userId + "$$$" + currentUser.genNumber);

        userId = HttpUtility.UrlEncode(userId.Replace("+", "25252"));
        string url = Url.Action("genology", "Home") + "?a=" + userId;
                        }
                        <div class=""><a href="@url">@currentUser.userId</a> </div>
                        <div class="">
                            <span>@currentUser.firstname @currentUser.lastname</span>
                        </div>

                        <div class="greenrow">
                            L: @currentUser.leftActiveCount  R: @currentUser.rightActiveCount
                        </div>
                        <div class="redrow">
                            L: @currentUser.leftInactiveCount R: @currentUser.rightInactiveCount
                        </div>

                    </div>
                </div>
            </div>
            <br />
            <div class="row">
                <div class="col-md-offset-3 col-md-6">

                    <img width="100%" height="100%" src="~/Content/images/level1.PNG">

                </div>
            </div>
            <br />
            <!--level2-->

            <div class="row">
                @{


        if (leftUser != null)
        {
            @renderNode(leftUser, "col-md-offset-2 col-md-2");
            <!--
            <div class="col-md-offset-2 col-md-2">
                <div style="background-color:white;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>

                    @{
            userId = algo.Encrypt(leftUser.userId + "$$$" + leftUser.genNumber);

            userId = HttpUtility.UrlEncode(userId.Replace("+", "25252"));
             url = Url.Action("genology", "Home") + "?a=" + userId;
                    }

                    <div class=""><a href="@url">@leftUser.userId</a> </div>

                    <div class="">
                        <span>@leftUser.firstname @leftUser.lastname</span>
                    </div>

                    <div class="greenrow">
                        L: @leftUser.leftActiveCount  R: @leftUser.rightActiveCount
                    </div>
                    <div class="redrow">
                        L: @leftUser.leftInactiveCount R: @leftUser.rightInactiveCount
                    </div>

                </div>
            </div>
                    -->
        }
        else
        {
            url = "#";
            string content = "";
            if (loginUser.credit >= 250)
            {
                if (currentUser != null)
                {
                    //parent gen number , my position , parent user id 
                    content = currentUser.genNumber + "$$$left$$$" + currentUser.userId;
                    content =algo.Encrypt(content);
                    content = HttpUtility.UrlEncode(content.Replace("+", "25252"));
                    url = Url.Action("SignUp", "Account") + "?a="+content;
                }
            }
            <div class="col-md-offset-2 col-md-2">
                <div style=" background-color:gray;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>


                    <div class="">
                        <span>Empty</span>
                    </div>
                    <div class="">
                        <span><a href="@url">Add One</a></span>
                    </div>

                </div>
            </div>
        }
                }


                @{

        if (rightUser != null)
        {
            <div class="col-md-offset-4 col-md-2">
                <div style="background-color:white;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>

                    @{
            userId = algo.Encrypt(rightUser.userId + "$$$" + rightUser.genNumber);

            userId = HttpUtility.UrlEncode(userId.Replace("+", "25252"));
            url = Url.Action("genology", "Home") + "?a=" + userId;
                    }
                    <div class=""><a href="@url">@rightUser.userId</a> </div>

                    <div class="">
                        <span>@rightUser.firstname @rightUser.lastname</span>
                    </div>

                    <div class="greenrow">
                        L: @rightUser.leftActiveCount  R: @rightUser.rightActiveCount
                    </div>
                    <div class="redrow">
                        L: @rightUser.leftInactiveCount R: @rightUser.rightInactiveCount
                    </div>

                </div>
            </div>
        }
        else
        {
            url = "#";
            string content = "";
            if (loginUser.credit >= 250)
            {
                if (currentUser != null)
                {
                    //parent gen number , my position , parent user id 
                    content = currentUser.genNumber + "$$$right$$$" + currentUser.userId;
                    content = algo.Encrypt(content);
                    content = HttpUtility.UrlEncode(content.Replace("+", "25252"));
                    url = Url.Action("SignUp", "Account") + "?a=" + content;
                }
            }
            <div class="col-md-offset-4 col-md-2">
                <div style=" background-color:gray;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>


                    <div class="">
                        <span>Empty</span>
                    </div>
                    <div class="">
                        <span><a href="@url">Add One</a></span>
                    </div>

                </div>
            </div>
        }
                }
            </div>
            <br />
            <div class="row">

                <div class="col-md-offset-1 col-md-4">

                    <img width="100%" height="100%" src="~/Content/images/level1.PNG">

                </div>


                <div class="col-md-offset-2 col-md-4">

                    <img width="100%" height="100%" src="~/Content/images/level1.PNG">

                </div>


            </div>
            <br />
            <!--level 3-->
            <div class="row">
                @{
        extreemt.user leftLeftUser = null;
        extreemt.user leftRightUser = null;
        currentUser = leftUser;
        if (currentUser != null)
        {
            if (currentUser.genNumber == 1)
            {
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 2).Count() > 0)
                {

                    leftLeftUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 2).First();
                }
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 3).Count() > 0)
                {

                    leftRightUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 3).First();
                }
            }
            else if (currentUser.genNumber == 2)
            {
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 2).Count() > 0)
                {

                    leftLeftUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 2).First();
                }
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 2).Count() > 0)
                {

                    leftRightUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 2).First();
                }
            }
            else
            {

                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 3).Count() > 0)
                {

                    leftLeftUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 3).First();
                }
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 3).Count() > 0)
                {

                    leftRightUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 3).First();
                }
            }

        }






        if (leftLeftUser != null)
        {
            <div class="col-md-offset-3-arb3 col-md-1-nos" style="">
                <div style="background-color:white;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>


                    @{
            userId = algo.Encrypt(leftLeftUser.userId + "$$$" + leftLeftUser.genNumber);
            userId = HttpUtility.UrlEncode(userId.Replace("+", "25252"));
            url = Url.Action("genology", "Home") + "?a=" + userId;
                    }
                    <div class=""><a href="@url">@leftLeftUser.userId</a> </div>
                    <div class="">
                        <span>@leftLeftUser.firstname @leftLeftUser.lastname</span>
                    </div>

                    <div class="greenrow">
                        L: @leftLeftUser.leftActiveCount  R: @leftLeftUser.rightActiveCount
                    </div>
                    <div class="redrow">
                        L: @leftLeftUser.leftInactiveCount R: @leftLeftUser.rightInactiveCount
                    </div>

                </div>
            </div>
        }
        else
        {
            url = "#";
            string content = "";
            if (loginUser.credit >= 250)
            {
                if (currentUser != null)
                {
                    //parent gen number , my position , parent user id 
                    content = currentUser.genNumber + "$$$left$$$" + currentUser.userId;
                    content = algo.Encrypt(content);
                    content = HttpUtility.UrlEncode(content.Replace("+", "25252"));
                    url = Url.Action("SignUp", "Account") + "?a=" + content;
                }
            }
            <div class="col-md-offset-3-arb3 col-md-1-nos">
                <div style=" background-color:gray;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>


                    <div class="">
                        <span>Empty</span>
                    </div>
                    <div class="">
                        <span><a href="@url">Add One</a></span>
                    </div>

                </div>
            </div>
        }

        if (leftRightUser != null)
        {
            <div class="col-md-offset-1-nos col-md-1-nos">
                <div style="background-color:white;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>

                    @{
            userId = algo.Encrypt(leftRightUser.userId + "$$$" + leftRightUser.genNumber);
            userId = HttpUtility.UrlEncode(userId.Replace("+", "25252"));
            url = Url.Action("genology", "Home") + "?a=" + userId;
                    }
                    <div class=""><a href="@url">@leftRightUser.userId</a> </div>

                    <div class="">
                        <span>@leftRightUser.firstname @leftRightUser.lastname</span>
                    </div>

                    <div class="greenrow">
                        L: @leftRightUser.leftActiveCount  R: @leftRightUser.rightActiveCount
                    </div>
                    <div class="redrow">
                        L: @leftRightUser.leftInactiveCount R: @leftRightUser.rightInactiveCount
                    </div>

                </div>
            </div>
        }
        else
        {
            url = "#";
            string content = "";
            if (loginUser.credit >= 250)
            {
                if (currentUser != null)
                {
                    //parent gen number , my position , parent user id 
                    content = currentUser.genNumber + "$$$right$$$" + currentUser.userId;
                    content = algo.Encrypt(content);
                    content = HttpUtility.UrlEncode(content.Replace("+", "25252"));
                    url = Url.Action("SignUp", "Account") + "?a=" + content;
                }
            }
            <div class="col-md-offset-1-nos col-md-1-nos">
                <div style=" background-color:gray;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>


                    <div class="">
                        <span>Empty</span>
                    </div>

                    <div class="">
                        <span><a href="@url">Add One</a></span>
                    </div>
                </div>
            </div>
        }
                }
                @{
        extreemt.user rightLeftUser = null;
        extreemt.user rightRightUser = null;
        currentUser = rightUser;
        if (currentUser != null)
        {
            if (currentUser.genNumber == 1)
            {
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 2).Count() > 0)
                {

                    rightLeftUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 2).First();
                }
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 3).Count() > 0)
                {

                    rightRightUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 3).First();
                }
            }
            else if (currentUser.genNumber == 2)
            {
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 2).Count() > 0)
                {

                    rightLeftUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 2).First();
                }
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 2).Count() > 0)
                {

                    rightRightUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 2).First();
                }
            }
            else
            {

                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 3).Count() > 0)
                {

                    rightLeftUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 3).First();
                }
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 3).Count() > 0)
                {

                    rightRightUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 3).First();
                }
            }



        }

        if (rightLeftUser != null)
        {
            <div class="col-md-offset-1-nos col-md-1-nos">
                <div style="background-color:white;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>

                    @{
            userId = algo.Encrypt(rightLeftUser.userId + "$$$" + rightLeftUser.genNumber);
            userId = HttpUtility.UrlEncode(userId.Replace("+", "25252"));
            url = Url.Action("genology", "Home") + "?a=" + userId;
                    }

                    <div class=""><a href="@url">@rightLeftUser.userId</a> </div>
                    <div class="">
                        <span>@rightLeftUser.firstname @rightLeftUser.lastname</span>
                    </div>

                    <div class="greenrow">
                        L: @rightLeftUser.leftActiveCount  R: @rightLeftUser.rightActiveCount
                    </div>
                    <div class="redrow">
                        L: @rightLeftUser.leftInactiveCount R: @rightLeftUser.rightInactiveCount
                    </div>

                </div>
            </div>
        }
        else
        {
            url = "#";
            string content = "";
            if (loginUser.credit >= 250)
            {
                if (currentUser != null)
                {
                    //parent gen number , my position , parent user id 
                    content = currentUser.genNumber + "$$$left$$$" + currentUser.userId;
                    content = algo.Encrypt(content);
                    content = HttpUtility.UrlEncode(content.Replace("+", "25252"));
                    url = Url.Action("SignUp", "Account") + "?a=" + content;
                }
            }
            <div class="col-md-offset-1-nos col-md-1-nos">
                <div style=" background-color:gray;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>


                    <div class="">
                        <span>Empty</span>
                    </div>
                    <div class="">
                        <span><a href="@url">Add One</a></span>
                    </div>

                </div>
            </div>
        }

        if (rightRightUser != null)
        {
            <div class="col-md-offset-1-nos col-md-1-nos">
                <div style="background-color:white;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>

                    @{
            userId = algo.Encrypt(rightRightUser.userId + "$$$" + rightRightUser.genNumber);
            userId = HttpUtility.UrlEncode(userId.Replace("+", "25252"));
            url = Url.Action("genology", "Home") + "?a=" + userId;
                    }
                    <div class=""><a href="@url">@rightRightUser.userId</a> </div>
                    <div class="">
                        <span>@rightRightUser.firstname @rightRightUser.lastname</span>
                    </div>

                    <div class="greenrow">
                        L: @rightRightUser.leftActiveCount  R: @rightRightUser.rightActiveCount
                    </div>
                    <div class="redrow">
                        L: @rightRightUser.leftInactiveCount R: @rightRightUser.rightInactiveCount
                    </div>

                </div>
            </div>
        }
        else
        {
            url = "#";
            string content = "";
            if (loginUser.credit >= 250)
            {
                if (currentUser != null)
                {
                    //parent gen number , my position , parent user id 
                    content = currentUser.genNumber + "$$$right$$$" + currentUser.userId;
                    content = algo.Encrypt(content);
                    content = HttpUtility.UrlEncode(content.Replace("+", "25252"));
                    url = Url.Action("SignUp", "Account") + "?a=" + content;
                }
            }
            <div class="col-md-offset-1-nos col-md-1-nos">
                <div style=" background-color:gray;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>


                    <div class="">
                        <span>Empty</span>
                    </div>
                    <div class="">
                        <span><a href="@url">Add One</a></span>
                    </div>

                </div>
            </div>
        }
                }

            </div>
            <br />
            <div class="row">

                <div class="col-md-2" style="margin-left:3.42599%">

                    <img width="100%" height="100%" src="~/Content/images/level1.PNG">

                </div>


                <div class="col-md-offset-1 col-md-2">

                    <img width="100%" height="100%" src="~/Content/images/level1.PNG">

                </div>
                <div class="col-md-offset-1 col-md-2">

                    <img width="100%" height="100%" src="~/Content/images/level1.PNG">

                </div>


                <div class="col-md-offset-1 col-md-2">

                    <img width="100%" height="100%" src="~/Content/images/level1.PNG">

                </div>

            </div>
            <br />

            <!--level 4-->
            <div class="row">

                @{
        extreemt.user leftLeftLeftUser = null;
        extreemt.user leftLeftRightUser = null;
        currentUser = leftLeftUser;
        if (currentUser != null)
        {
            if (currentUser.genNumber == 1)
            {
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 2).Count() > 0)
                {

                    leftLeftLeftUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 2).First();
                }
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 3).Count() > 0)
                {

                    leftLeftRightUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 3).First();
                }
            }
            else if (currentUser.genNumber == 2)
            {
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 2).Count() > 0)
                {

                    leftLeftLeftUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 2).First();
                }
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 2).Count() > 0)
                {

                    leftLeftRightUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 2).First();
                }
            }
            else
            {

                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 3).Count() > 0)
                {

                    leftLeftLeftUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 3).First();
                }
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 3).Count() > 0)
                {

                    leftLeftRightUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 3).First();
                }
            }



        }

        if (leftLeftLeftUser != null)
        {
            <div class="col-md-1-nos">
                <div style="background-color:white;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>

                    @{
            userId = algo.Encrypt(leftLeftLeftUser.userId + "$$$" + leftLeftLeftUser.genNumber);
            userId = HttpUtility.UrlEncode(userId.Replace("+", "25252"));
            url = Url.Action("genology", "Home") + "?a=" + userId;
                    }
                    <div class=""><a href="@url">@leftLeftLeftUser.userId</a> </div>
                    <div class="">
                        <span>@leftLeftLeftUser.firstname @leftLeftLeftUser.lastname</span>
                    </div>

                    <div class="greenrow">
                        L: @leftLeftLeftUser.leftActiveCount  R: @leftLeftLeftUser.rightActiveCount
                    </div>
                    <div class="redrow">
                        L: @leftLeftLeftUser.leftInactiveCount R: @leftLeftLeftUser.rightInactiveCount
                    </div>

                </div>
            </div>
        }
        else
        {
            url = "#";
            string content = "";
            if (loginUser.credit >= 250)
            {
                if (currentUser != null)
                {
                    //parent gen number , my position , parent user id 
                    content = currentUser.genNumber + "$$$left$$$" + currentUser.userId;
                    content = algo.Encrypt(content);
                    content = HttpUtility.UrlEncode(content.Replace("+", "25252"));
                    url = Url.Action("SignUp", "Account") + "?a=" + content;
                }
            }
            <div class="col-md-1-nos">
                <div style=" background-color:gray;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>


                    <div class="">
                        <span>Empty</span>
                    </div>
                    <div class="">
                        <span><a href="@url">Add One</a></span>
                    </div>

                </div>
            </div>
        }

        if (leftLeftRightUser != null)
        {
            <div class="col-md-1-nos">
                <div style="background-color:white;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>

                    @{
            userId = algo.Encrypt(leftLeftRightUser.userId + "$$$" + leftLeftRightUser.genNumber);
            userId = HttpUtility.UrlEncode(userId.Replace("+", "25252"));
            url = Url.Action("genology", "Home") + "?a=" + userId;
                    }
                    <div class=""><a href="@url">@leftLeftRightUser.userId</a> </div>
                    <div class="">
                        <span>@leftLeftRightUser.firstname @leftLeftRightUser.lastname</span>
                    </div>

                    <div class="greenrow">
                        L: @leftLeftRightUser.leftActiveCount  R: @leftLeftRightUser.rightActiveCount
                    </div>
                    <div class="redrow">
                        L: @leftLeftRightUser.leftInactiveCount R: @leftLeftRightUser.rightInactiveCount
                    </div>

                </div>
            </div>
        }
        else
        {
            url = "#";
            string content = "";
            if (loginUser.credit >= 250)
            {
                if (currentUser != null)
                {
                    //parent gen number , my position , parent user id 
                    content = currentUser.genNumber + "$$$right$$$" + currentUser.userId;
                    content = algo.Encrypt(content);
                    content = HttpUtility.UrlEncode(content.Replace("+", "25252"));
                    url = Url.Action("SignUp", "Account") + "?a=" + content;
                }
            }
            <div class="col-md-1-nos">
                <div style=" background-color:gray;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>


                    <div class="">
                        <span>Empty</span>
                    </div>

                    <div class="">
                        <span><a href="@url">Add One</a></span>
                    </div>
                </div>
            </div>
        }
                }

                @{
        extreemt.user leftRightLeftUser = null;
        extreemt.user leftRightRightUser = null;
        currentUser = leftRightUser;
        if (currentUser != null)
        {
            if (currentUser.genNumber == 1)
            {
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 2).Count() > 0)
                {

                    leftRightLeftUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 2).First();
                }
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 3).Count() > 0)
                {

                    leftRightRightUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 3).First();
                }
            }
            else if (currentUser.genNumber == 2)
            {
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 2).Count() > 0)
                {

                    leftRightLeftUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 2).First();
                }
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 2).Count() > 0)
                {

                    leftRightRightUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 2).First();
                }
            }
            else
            {

                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 3).Count() > 0)
                {

                    leftRightLeftUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 3).First();
                }
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 3).Count() > 0)
                {

                    leftRightRightUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 3).First();
                }
            }



        }

        if (leftRightLeftUser != null)
        {
            <div class="col-md-1-nos">
                <div style="background-color:white;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>

                    @{
            userId = algo.Encrypt(leftRightLeftUser.userId + "$$$" + leftRightLeftUser.genNumber);
            userId = HttpUtility.UrlEncode(userId.Replace("+", "25252"));
            url = Url.Action("genology", "Home") + "?a=" + userId;
                    }
                    <div class=""><a href="@url">@leftRightLeftUser.userId</a> </div>
                    <div class="">
                        <span>@leftRightLeftUser.firstname @leftRightLeftUser.lastname</span>
                    </div>

                    <div class="greenrow">
                        L: @leftRightLeftUser.leftActiveCount  R: @leftRightLeftUser.rightActiveCount
                    </div>
                    <div class="redrow">
                        L: @leftRightLeftUser.leftInactiveCount R: @leftRightLeftUser.rightInactiveCount
                    </div>

                </div>
            </div>
        }
        else
        {
            url = "#";
            string content = "";
            if (loginUser.credit >= 250)
            {
                if (currentUser != null)
                {
                    //parent gen number , my position , parent user id 
                    content = currentUser.genNumber + "$$$left$$$" + currentUser.userId;
                    content = algo.Encrypt(content);
                    content = HttpUtility.UrlEncode(content.Replace("+", "25252"));
                    url = Url.Action("SignUp", "Account") + "?a=" + content;
                }
            }
            <div class="col-md-1-nos">
                <div style=" background-color:gray;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>


                    <div class="">
                        <span>Empty</span>
                    </div>
                    <div class="">
                        <span><a href="@url">Add One</a></span>
                    </div>

                </div>
            </div>
        }

        if (leftRightRightUser != null)
        {
            <div class="col-md-1-nos">
                <div style="background-color:white;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>

                    @{
            userId = algo.Encrypt(leftRightRightUser.userId + "$$$" + leftRightRightUser.genNumber);
            userId = HttpUtility.UrlEncode(userId.Replace("+", "25252"));
            url = Url.Action("genology", "Home") + "?a=" + userId;
                    }
                    <div class=""><a href="@url">@leftRightRightUser.userId</a> </div>
                    <div class="">
                        <span>@leftRightRightUser.firstname @leftRightRightUser.lastname</span>
                    </div>

                    <div class="greenrow">
                        L: @leftRightRightUser.leftActiveCount  R: @leftRightRightUser.rightActiveCount
                    </div>
                    <div class="redrow">
                        L: @leftRightRightUser.leftInactiveCount R: @leftRightRightUser.rightInactiveCount
                    </div>

                </div>
            </div>
        }
        else
        {
            url = "#";
            string content = "";
            if (loginUser.credit >= 250)
            {
                if (currentUser != null)
                {
                    //parent gen number , my position , parent user id 
                    content = currentUser.genNumber + "$$$right$$$" + currentUser.userId;
                    content = algo.Encrypt(content);
                    content = HttpUtility.UrlEncode(content.Replace("+", "25252"));
                    url = Url.Action("SignUp", "Account") + "?a=" + content;
                }
            }
            <div class="col-md-1-nos">
                <div style=" background-color:gray;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>


                    <div class="">
                        <span>Empty</span>
                    </div>
                    <div class="">
                        <span><a href="@url">Add One</a></span>
                    </div>

                </div>
            </div>
        }
                }

                @{
        extreemt.user rightLeftLeftUser = null;
        extreemt.user rightLeftRightUser = null;
        currentUser = rightLeftUser;
        if (currentUser != null)
        {
            if (currentUser.genNumber == 1)
            {
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 2).Count() > 0)
                {

                    rightLeftLeftUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 2).First();
                }
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 3).Count() > 0)
                {

                    rightLeftRightUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 3).First();
                }
            }
            else if (currentUser.genNumber == 2)
            {
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 2).Count() > 0)
                {

                    rightLeftLeftUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 2).First();
                }
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 2).Count() > 0)
                {

                    rightLeftRightUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 2).First();
                }
            }
            else
            {

                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 3).Count() > 0)
                {

                    rightLeftLeftUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 3).First();
                }
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 3).Count() > 0)
                {

                    rightLeftRightUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 3).First();
                }
            }



        }

        if (rightLeftLeftUser != null)
        {
            <div class="col-md-1-nos">
                <div style="background-color:white;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>
                    @{
            userId = algo.Encrypt(rightLeftLeftUser.userId + "$$$" + rightLeftLeftUser.genNumber);
            userId = HttpUtility.UrlEncode(userId.Replace("+", "25252"));
            url = Url.Action("genology", "Home") + "?a=" + userId;
                    }
                    <div class=""><a href="@url">@rightLeftLeftUser.userId</a> </div>
                    <div class="">
                        <span>@rightLeftLeftUser.firstname @rightLeftLeftUser.lastname</span>
                    </div>

                    <div class="greenrow">
                        L: @rightLeftLeftUser.leftActiveCount  R: @rightLeftLeftUser.rightActiveCount
                    </div>
                    <div class="redrow">
                        L: @rightLeftLeftUser.leftInactiveCount R: @rightLeftLeftUser.rightInactiveCount
                    </div>

                </div>
            </div>
        }
        else
        {
            url = "#";
            string content = "";
            if (loginUser.credit >= 250)
            {
                if (currentUser != null)
                {
                    //parent gen number , my position , parent user id 
                    content = currentUser.genNumber + "$$$left$$$" + currentUser.userId;
                    content = algo.Encrypt(content);
                    content = HttpUtility.UrlEncode(content.Replace("+", "25252"));
                    url = Url.Action("SignUp", "Account") + "?a=" + content;
                }
            }
            <div class="col-md-1-nos">
                <div style=" background-color:gray;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>


                    <div class="">
                        <span>Empty</span>
                    </div>
                    <div class="">
                        <span><a href="@url">Add One</a></span>
                    </div>

                </div>


            </div>
        }

        if (rightLeftRightUser != null)
        {
            <div class="col-md-1-nos">
                <div style="background-color:white;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>
                    @{
            userId = algo.Encrypt(rightLeftRightUser.userId + "$$$" + rightLeftRightUser.genNumber);
            userId = HttpUtility.UrlEncode(userId.Replace("+", "25252"));
            url = Url.Action("genology", "Home") + "?a=" + userId;
                    }
                    <div class=""><a href="@url">@rightLeftRightUser.userId</a> </div>
                    <div class="">
                        <span>@rightLeftRightUser.firstname @rightLeftRightUser.lastname</span>
                    </div>

                    <div class="greenrow">
                        L: @rightLeftRightUser.leftActiveCount  R: @rightLeftRightUser.rightActiveCount
                    </div>
                    <div class="redrow">
                        L: @rightLeftRightUser.leftInactiveCount R: @rightLeftRightUser.rightInactiveCount
                    </div>

                </div>
            </div>
        }
        else
        {
            url = "#";
            string content = "";
            if (loginUser.credit >= 250)
            {
                if (currentUser != null)
                {
                    //parent gen number , my position , parent user id 
                    content = currentUser.genNumber + "$$$right$$$" + currentUser.userId;
                    content = algo.Encrypt(content);
                    content = HttpUtility.UrlEncode(content.Replace("+", "25252"));
                    url = Url.Action("SignUp", "Account") + "?a=" + content;
                }
            }
            <div class="col-md-1-nos">
                <div style=" background-color:gray;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>


                    <div class="">
                        <span>Empty</span>
                    </div>
                    <div class="">
                        <span><a href="@url">Add One</a></span>
                    </div>

                </div>
            </div>
        }
                }
                @{
        extreemt.user rightRightLeftUser = null;
        extreemt.user rightRightRightUser = null;
        currentUser = rightRightUser;
        if (currentUser != null)
        {
            if (currentUser.genNumber == 1)
            {
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 2).Count() > 0)
                {

                    rightRightLeftUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 2).First();
                }
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 3).Count() > 0)
                {

                    rightRightRightUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 3).First();
                }
            }
            else if (currentUser.genNumber == 2)
            {
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 2).Count() > 0)
                {

                    rightRightLeftUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 2).First();
                }
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 2).Count() > 0)
                {

                    rightRightRightUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 2).First();
                }
            }
            else
            {

                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 3).Count() > 0)
                {

                    rightRightLeftUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "left" && u.parentGenNum == 3).First();
                }
                if (db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 3).Count() > 0)
                {

                    rightRightRightUser = db.users.Where(u => u.parentUserId == currentUser.userId && u.genNumber == 1 && u.position == "right" && u.parentGenNum == 3).First();
                }
            }



        }

        if (rightRightLeftUser != null)
        {
            <div class="col-md-1-nos">
                <div style="background-color:white;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>

                    @{
            userId = algo.Encrypt(rightRightLeftUser.userId + "$$$" + rightRightLeftUser.genNumber);
            userId = HttpUtility.UrlEncode(userId.Replace("+", "25252"));
            url = Url.Action("genology", "Home") + "?a=" + userId;
                    }
                    <div class=""><a href="@url">@rightRightLeftUser.userId</a> </div>
                    <div class="">
                        <span>@rightRightLeftUser.firstname @rightRightLeftUser.lastname</span>
                    </div>

                    <div class="greenrow">
                        L: @rightRightLeftUser.leftActiveCount  R: @rightRightLeftUser.rightActiveCount
                    </div>
                    <div class="redrow">
                        L: @rightRightLeftUser.leftInactiveCount R: @rightRightLeftUser.rightInactiveCount
                    </div>

                </div>
            </div>
        }
        else
        {
            url = "#";
            string content = "";
            if (loginUser.credit >= 250)
            {
                if (currentUser != null)
                {
                    //parent gen number , my position , parent user id 
                    content = currentUser.genNumber + "$$$left$$$" + currentUser.userId;
                    content = algo.Encrypt(content);
                    content = HttpUtility.UrlEncode(content.Replace("+", "25252"));
                    url = Url.Action("SignUp", "Account") + "?a=" + content;
                }
            }
            <div class="col-md-1-nos">
                <div style=" background-color:gray;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>


                    <div class="">
                        <span>Empty</span>
                    </div>
                    <div class="">
                        <span><a href="@url">Add One</a></span>
                    </div>

                </div>
            </div>
        }

        if (rightRightRightUser != null)
        {
            <div class="col-md-1-nos">
                <div style="background-color:white;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>

                    @{
            userId = algo.Encrypt(rightRightRightUser.userId + "$$$" + rightRightRightUser.genNumber);
            userId = HttpUtility.UrlEncode(userId.Replace("+", "25252"));
            url = Url.Action("genology", "Home") + "?a=" + userId;
                    }
                    <div class=""><a href="@url">@rightRightRightUser.userId</a> </div>
                    <div class="">
                        <span>@rightRightRightUser.firstname @rightRightRightUser.lastname</span>
                    </div>

                    <div class="greenrow">
                        L: @rightRightRightUser.leftActiveCount  R: @rightRightRightUser.rightActiveCount
                    </div>
                    <div class="redrow">
                        L: @rightRightRightUser.leftInactiveCount R: @rightRightRightUser.rightInactiveCount
                    </div>

                </div>
            </div>
        }
        else
        {
            url = "#";
            string content = "";
            if (loginUser.credit >= 250)
            {
                if (currentUser != null)
                {
                    //parent gen number , my position , parent user id 
                    content = currentUser.genNumber + "$$$right$$$" + currentUser.userId;
                    content = algo.Encrypt(content);
                    content = HttpUtility.UrlEncode(content.Replace("+", "25252"));
                    url = Url.Action("SignUp", "Account") + "?a=" + content;
                }
            }
            <div class="col-md-1-nos" style="">
                <div style=" background-color:gray;text-align:center;border-style:solid;border-radius:10px">

                    <div class="">
                        <img src="~/Content/images/images.jpg" width="60" height="60" style="border: 2px solid gray;">
                    </div>


                    <div class="">
                        <span>Empty</span>
                    </div>
                    <div class="">
                        <span><a href="@url">Add One</a></span>
                    </div>

                </div>
            </div>
        }
                }
            </div>
            <br />
        </div>

    }
}
